services:
  - type: web
    name: varunexa-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.contact:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
